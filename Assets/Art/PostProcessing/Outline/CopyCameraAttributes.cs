using System.Collections;
using System.Collections.Generic;
using UnityEngine;


/// <summary>
/// Helper script to adjust the clone camera used for the highlighted normals effect, when the main camera resizes
/// </summary>
public class CopyCameraAttributes : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
